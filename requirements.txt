numpy==1.23.5
pandas==1.5.3
matplotlib==3.6.2
pandas-datareader==0.10.0
yfinance==0.2.18
tensorflow==2.11.0
keras==2.11.0
streamlit==1.16.0
scikit-learn==1.1.3
prophet==1.2
plotly==5.10.0


To create this file:
1.Create a new file named requirements.txt in your project directory.
2.Copy the above content into that file.

You can then use this file to install the required dependencies by running:

        pip install -r requirements.txt

This command will ensure all the necessary packages are installed for your project.